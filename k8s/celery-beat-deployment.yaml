# ------------------ Kubernetes Celery Beat Scheduler Deployment -----------------------
# Deployment manifest for single-replica Celery Beat scheduler pod.
# Celery Beat periodically triggers cron tasks (periodic market price polling).
# IMPORTANT: Must always run with 'replicas: 1' to avoid duplicate scheduled tasks!
# --------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-celery-beat
  namespace: market-service
  labels:
    app: market-service
    component: celery-beat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: market-service
      component: celery-beat
  template:
    metadata:
      labels:
        app: market-service
        component: celery-beat
    spec:
      containers:
        # ------------------ Celery Beat Scheduler Process -----------------------
        - name: celery-beat
          image: market-service:latest
          imagePullPolicy: IfNotPresent
          command: ["celery", "-A", "market_service.core.celery", "beat", "--loglevel=info"]
          envFrom:
            - configMapRef:
                name: market-service-config
            - secretRef:
                name: market-service-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
